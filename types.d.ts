export type Book = {
    // id: number
    title: string;
    author: string;
    rating: string; // Assuming rate is represented as a string for simplicity
    img: string;
    description: string;
};